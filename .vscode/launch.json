{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Dev Container: Listen for Xdebug",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "pathMappings": {
        "/workspaces/portland-art-supply": "${workspaceFolder}"
      }
    }
  ]
}
