services:
  portland-art-supply:
    build: .
    ports:
      - "8081:80"
    volumes:
      - ./.env:/var/www/html/.env:ro
networks:
  pas_net:
    name: pas_net
    driver: bridge
    external: true
